"use client";
import { X } from "lucide-react";
import type { Recipe } from "@/lib/kitchen/types";
/** 菜谱详情弹窗，展示菜谱信息并将操作交回父组件。 */
export function RecipeModal({ recipe, isOrderedByCurrentMember, onClose, onToggle }: { recipe: Recipe | null; isOrderedByCurrentMember: boolean; onClose: () => void; onToggle: (recipe: Recipe) => void }) {
  if (!recipe) return null;
  return <div className="fixed inset-0 z-50 bg-black/60 backdrop-blur-xs flex items-center justify-center p-4 overflow-y-auto"><div className="bg-white rounded-3xl max-w-lg w-full max-h-[90vh] overflow-y-auto p-6 space-y-5 shadow-2xl border border-stone-200 animate-fade-in"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="text-xl font-bold text-stone-900">{recipe.name}</h3><span className="text-xs px-2 py-0.5 rounded-md bg-orange-100 text-orange-800 font-medium">{recipe.category}</span></div>{recipe.description && <p className="text-xs text-stone-500 mt-1">{recipe.description}</p>}</div><button onClick={onClose} className="p-1 rounded-full text-stone-400 hover:bg-stone-100 hover:text-stone-700"><X className="w-5 h-5" /></button></div><div><h4 className="text-xs font-bold text-stone-600 uppercase tracking-wider mb-2">所需食材</h4><div className="grid grid-cols-2 gap-2">{recipe.ingredients.map((ing, i) => <div key={i} className="p-2 rounded-lg bg-stone-50 border border-stone-200 text-xs flex items-center justify-between"><span className="font-medium text-stone-800">{ing.name}</span><span className="text-orange-600">{ing.quantity ? `${ing.quantity}${ing.unit}` : ing.unit}</span></div>)}</div></div><div><h4 className="text-xs font-bold text-stone-600 uppercase tracking-wider mb-2">烹饪步骤</h4><div className="space-y-2.5">{recipe.steps.map(st => <div key={st.step} className="p-3 bg-stone-50 rounded-xl text-xs space-y-1"><div className="font-bold text-stone-900 flex items-center gap-1.5"><span className="w-4 h-4 rounded-full bg-stone-900 text-white text-[10px] flex items-center justify-center">{st.step}</span><span>{st.title}</span></div><p className="text-stone-600 pl-5">{st.content}</p>{st.tip && <p className="text-amber-700 text-[11px] pl-5">💡 提示: {st.tip}</p>}</div>)}</div></div><div className="pt-3 border-t border-stone-100 flex items-center justify-end gap-2"><button onClick={onClose} className="px-4 py-2 text-xs font-medium text-stone-600 hover:bg-stone-100 rounded-xl">关闭</button><button onClick={() => { onToggle(recipe); onClose(); }} className="px-4 py-2 bg-orange-500 hover:bg-orange-600 text-white text-xs font-semibold rounded-xl shadow-xs">{isOrderedByCurrentMember ? "从今日菜单移除" : "加入今日菜单"}</button></div></div></div>;
}
